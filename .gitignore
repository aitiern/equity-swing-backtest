__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/
.env
.streamlit/secrets.toml
.DS_Store
# Run outputs (the blotter etc.) are reproducible — don't commit them:
results/

# Keep raw data out of git unless it's small and you have the right to publish it:
data/*.csv
!data/.gitkeep
